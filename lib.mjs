export const repeat = (string) => `${string} ${string}`;
// export function shout(string) {
//   return `${string.toUpperCase()}!`;
// }
export function shout(string) {
  console.log('hbjnmk');
  return `${string.toUpperCase()}!`;
}
